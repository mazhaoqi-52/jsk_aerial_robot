<?xml version="1.0"?>
<launch>
  <!-- 参数设置 -->
  <arg name="module_ids" default="1,2"/>
  <arg name="real_machine" default="true"/>
  <arg name="simulation" default="false"/>
  <arg name="valve_ns" default="valve"/>
  <arg name="valve_id" default="1"/>
  <arg name="valve_x" default="3.0"/>
  <arg name="valve_y" default="0.0"/>
  <arg name="valve_yaw" default="0.0"/>
  
  <!-- 阀门位置发布 -->
  <include file="$(find beetle)/launch/valve_rotation_tasks/valve_pos.launch">
    <arg name="real_machine" value="$(arg real_machine)" />
    <arg name="simulation" value="$(arg simulation)" />
    <arg name="robot_ns" value="$(arg valve_ns)" />
    <arg name="robot_id" value="$(arg valve_id)"/>
    <arg name="valve_x" value="$(arg valve_x)" />
    <arg name="valve_y" value="$(arg valve_y)" />
    <arg name="valve_yaw" value="$(arg valve_yaw)" />
  </include>
  
  <!-- 使用现有的阀门操作状态机（已优化为抓取第2、3根横梁）-->
  <node pkg="beetle" type="valve_rotation_smach_test.py" 
        name="valve_manipulation_state_machine" output="screen">
    <!-- 基本参数 -->
    <param name="simulation" value="$(arg simulation)"/>
    <param name="module_ids" value="$(arg module_ids)"/>
    <param name="real_machine" value="$(arg real_machine)"/>
  </node>
  
  <!-- 可选：启动rviz用于可视化 -->
  <node pkg="rviz" type="rviz" name="valve_manipulation_rviz" 
        args="-d $(find beetle)/config/rviz_config/valve_manipulation.rviz" 
        if="$(arg simulation)" required="false"/>
        
</launch>
